image: maven:3.5-jdk-10

stages:
  - test
  - deploy


java:
  stage: test
  script:
    - mvn verify
  artifacts:
    reports:
      junit:
        - project/test/results/results.xml
      
      
pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r project/javadoc/* .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
